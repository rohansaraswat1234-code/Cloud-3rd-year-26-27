Linux commands used

pwd
cd Raja-GLA-CourseRegistration
ls -la
cat course_info.txt
tree

Git commands used

git init
git branch -M main
git status
git add .
git commit -m "Add initial course registration project files"
git checkout -b course-search
git add src/course_search.txt
git commit -m "Add course search module"
git checkout main
git merge course-search
git log --oneline --graph --all
